<!DOCTYPE html>
<html>
<head>
  <title>Band Notifier</title>
  <%= stylesheet_link_tag "web-app-theme/base", "web-app-theme/themes/drastic-dark/style", "web-app-theme/override","jquery-ui", :cache => true %>
  <%= javascript_include_tag :defaults, :cache => true %>
  <%= csrf_meta_tag %>
</head>
<body>
  <div id="container">
    <div id="header">
      <h1><a href="/">Band Notifier</a></h1>
      <div id="user-navigation">
          <ul class="wat-cf">
        <% if current_user -%>
            <li><%= link_to t("web-app-theme.profile", :default => "Profile"), edit_user_path(current_user.id) %></li>
            <li><a href="#"><%= t("web-app-theme.settings", :default => "Settings") %></a></li>
            <li><a href="/logout" class="logout"><%= t("web-app-theme.logout", :default => "Logout") %></a></li>
        <% else %>
            <li><a href="/login" class="login"><%= t("web-app-theme.login", :default => "Login") %></a></li>
        <% end %>
          </ul>
      </div>
      <div id="main-navigation">
        <ul class="wat-cf">
          <% if current_user -%>
            <li class="<%= controller.controller_path == 'events' ? 'active' : '' %>"><a href="<%= events_path %>">Events</a></li>
            <li class="<%= controller.controller_path == 'members' ? 'active' : '' %>"><a href="<%= members_path %>">Members</a></li>
            <% if current_user.admin? %>
              <li><%= link_to t('activerecord.models.users.plural'), users_path %></li>
            <% end %>
          <% else %>
            <li><%= link_to t('pages.home'), "/public/index" %></li>
            <li><%= link_to t('pages.info'), "/public/about" %></li>
          <% end %>
        </ul>
      </div>
    </div>
    <div id="wrapper" class="wat-cf">
      <div class="flash">
        <% flash.each do |type, message| -%>
          <div class="message <%= type %>">
            <p><%= message %></p>
          </div>
        <% end -%>
      </div>
      <div id="main">
        <div class="block">
          <div class="content">
            <%= yield %>
          </div>
        </div>
        <div id="footer">
          <div class="block">
            <p>Copyright &copy; <%= Time.now.year %> Band Notifier.</p>
          </div>
        </div>
      </div>
      <div id="sidebar">
        <%= yield :sidebar %>
      </div>
    </div>
  </div>
</body>
</html>
