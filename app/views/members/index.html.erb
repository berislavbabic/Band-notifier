<div class="block">
  <div class="secondary-navigation">
    <ul class="wat-cf">
      <li class="first active"><%= link_to "#{t("web-app-theme.list", :default => "List")}", members_path  %></li>
      <li><%= link_to "#{t("web-app-theme.new", :default => "New")}", new_member_path %></li>
    </ul>
  </div>          
  <div class="content">          
    <h2 class="title"><%= t("web-app-theme.all", :default => "All")  %> Members</h2>
    <div class="inner">
      <table class="table">
        <tr>             
          <th class="first">ID</th>
                    <th>
            <%= t("activerecord.attributes.member.first_name", :default => t("activerecord.labels.first_name", :default => "First_name")) %>
          </th>
                    <th><%= t("web-app-theme.created_at", :default => "Created at")  %></th>
          <th class="last">&nbsp;</th>
        </tr>
        <% @members.each do |member| -%>
        <tr class="<%= cycle("odd", "even") %>">
          <td>
            <%= member.id %>
          </td>
                    <td>
            <%= link_to member.first_name, member_path(member) %>
          </td>
                    <td>
            <%= member.created_at %>
          </td>
          <td class="last">
            <%= link_to "#{t("web-app-theme.show", :default => "Show")}", member_path(member) %> |
            <%= link_to "#{t("web-app-theme.edit", :default => "Edit")}", edit_member_path(member) %> |
            <%= link_to "#{t("web-app-theme.delete", :default => "Delete")}", member_path(member), :method => :delete, :confirm => "#{t("web-app-theme.confirm", :default => "Are you sure?")}" %>
          </td>
        </tr>          
        <% end -%>
      </table>
      <div class="actions-bar wat-cf">
        <div class="actions">
        </div>
        
      </div>
    </div>
  </div>
</div>

<% content_for :sidebar, render(:partial => 'sidebar') -%>
