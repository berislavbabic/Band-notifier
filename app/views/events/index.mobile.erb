<% title t('activerecord.models.events.plural') %>

<ul>
   <li>
        <%= t('activerecord.models.events.title') %> 
  </li>
  <% for event in @events %>
        <li class="arrow">
          <%= link_to event.name, event %>
          <small class="counter"><%= "#{event.event_date}" %> </small>
        </li>
  <% end %>
</ul>